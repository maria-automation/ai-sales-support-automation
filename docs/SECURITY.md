# Security

The public repository must not contain API keys, bot tokens, private Telegram IDs, private Google Sheet IDs or URLs, service-account credentials, live webhook IDs, n8n instance identifiers or real customer data.

The workflow in this repository replaces the sensitive values from the original export with placeholders.
